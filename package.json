{
  "name": "mandelbrot",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "yarn asbuild:debug && vite",
    "build": "yarn asbuild:release && tsc && vite build",
    "preview": "vite preview",
    "asbuild:debug": "asc as/assembly.ts --target debug",
    "asbuild:release": "asc as/assembly.ts --target release"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.3.9"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.1",
    "assemblyscript": "^0.23.1",
    "stimulus-vite-helpers": "^3.0.1"
  }
}
